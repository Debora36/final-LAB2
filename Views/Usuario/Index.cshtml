@model final_LAB2.Models.ViewModels.PaginatedListViewModel<final_LAB2.Models.Usuario>
@{
    ViewData["Title"] = "Gestión de Usuarios";
}

<div class="container-fluid">
    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="fas fa-check-circle me-2"></i>
            @TempData["SuccessMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <div class="row">
        <div class="col-12">
            <div class="card shadow-sm">
                <div class="card-header bg-white d-flex justify-content-between align-items-center py-3">
                    <h3 class="card-title mb-0 fw-bold text-primary">
                        <i class="fas fa-user-shield me-2"></i>Lista de Usuarios
                    </h3>
                    <a class="btn btn-primary" asp-action="Create">
                        <i class="fas fa-plus me-1"></i>Crear Usuario
                    </a>
                </div>
                <div class="card-body">
                    @if (Model.TotalCount > 0)
                    {
                        <p class="text-muted mb-3">
                            <i class="fas fa-info-circle me-1"></i>
                            Mostrando <strong>@((Model.PageIndex - 1) * Model.PageSize + 1)</strong> a
                            <strong>@Math.Min(Model.PageIndex * Model.PageSize, Model.TotalCount)</strong> de
                            <strong>@Model.TotalCount</strong> usuarios
                        </p>
                    }

                    <div class="table-responsive">
                        <table class="table table-hover align-middle">
                            <thead>
                                <tr>
                                    <th>ID</th>
                                    <th>Avatar</th>
                                    <th>Usuario</th>
                                    <th>Email</th>
                                    <th>Rol</th>
                                    <th>Estado</th>
                                    <th class="text-center">Acciones</th>
                                </tr>
                            </thead>
                            <tbody>
                                @if (Model.Items.Any())
                                {
                                    @foreach (var usuario in Model.Items)
                                    {
                                        <tr>
                                            <td><span class="badge bg-info">@usuario.Id</span></td>
                                            <td>
                                                <img src="@(string.IsNullOrWhiteSpace(usuario.AvatarUrl) ? "/img/avatar-default.svg" : usuario.AvatarUrl)"
                                                     alt="Avatar" class="rounded-circle" style="width: 36px; height: 36px; object-fit: cover;" />
                                            </td>
                                            <td><strong>@usuario.Username</strong></td>
                                            <td>@usuario.Email</td>
                                            <td><span class="badge bg-primary">@usuario.Rol</span></td>
                                            <td>
                                                @if (usuario.Activo)
                                                {
                                                    <span class="badge bg-success">Activo</span>
                                                }
                                                else
                                                {
                                                    <span class="badge bg-secondary">Inactivo</span>
                                                }
                                            </td>
                                            <td class="text-center">
                                                <a class="btn btn-sm btn-outline-warning" asp-action="Edit" asp-route-id="@usuario.Id" title="Editar">
                                                    <i class="fas fa-edit"></i>
                                                </a>
                                                @if (usuario.Activo)
                                                {
                                                    <form asp-action="Disable" asp-route-id="@usuario.Id" method="post" class="d-inline"
                                                          onsubmit="return confirm('¿Deshabilitar este usuario?');">
                                                        <button type="submit" class="btn btn-sm btn-outline-danger" title="Deshabilitar">
                                                            <i class="fas fa-user-slash"></i>
                                                        </button>
                                                    </form>
                                                }
                                                else
                                                {
                                                    <span class="btn btn-sm btn-outline-secondary disabled" title="Ya está deshabilitado">
                                                        <i class="fas fa-user-slash"></i>
                                                    </span>
                                                }
                                            </td>
                                        </tr>
                                    }
                                }
                                else
                                {
                                    <tr>
                                        <td colspan="7" class="text-center text-muted py-4">No hay usuarios registrados</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>

                    @if (Model.TotalPages > 1)
                    {
                        <nav aria-label="Navegación de páginas">
                            <ul class="pagination justify-content-center">
                                <li class="page-item @(!Model.HasPreviousPage ? "disabled" : "")">
                                    <a class="page-link" asp-action="Index" asp-route-pageIndex="@(Model.PageIndex - 1)">
                                        <i class="fas fa-angle-left"></i> Anterior
                                    </a>
                                </li>
                                @for (int i = 1; i <= Model.TotalPages; i++)
                                {
                                    <li class="page-item @(i == Model.PageIndex ? "active" : "")">
                                        <a class="page-link" asp-action="Index" asp-route-pageIndex="@i">@i</a>
                                    </li>
                                }
                                <li class="page-item @(!Model.HasNextPage ? "disabled" : "")">
                                    <a class="page-link" asp-action="Index" asp-route-pageIndex="@(Model.PageIndex + 1)">
                                        Siguiente <i class="fas fa-angle-right"></i>
                                    </a>
                                </li>
                            </ul>
                        </nav>
                    }
                </div>
            </div>
        </div>
    </div>
</div>
